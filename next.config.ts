import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  basePath: '/checklist',
  // assetPrefix: '/checklist', // Usually not needed with basePath, but good to keep in mind
};

export default nextConfig;
